<h1>Listing photos</h1>

<table>
  <tr>
    <th>Photo name</th>
	<th>Thumbnail</th>
    <th>File name</th>
    <th>Imageable id</th>
    <th>Imageable type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photos.each do |photo| %>
  <tr>
    <td><%= photo.photo_name %></td>
	<td><%= link_to(image_tag('/images/' + photo.file_name, :style=>"width:50px"),photo_path(photo.id) )%> </td>
	<td><%= link_to photo.file_name,"/photos/#{photo.id}"%></td>
    <td><%= photo.imageable_id %></td>
    <td><%= photo.imageable_type %></td>
    <td><%= link_to 'Show', photo %></td>
    <td><%= link_to 'Edit', edit_photo_path(photo) %></td>
    <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

New photos must be created at the league, team, or player level
