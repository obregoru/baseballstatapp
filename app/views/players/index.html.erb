<h1>Listing Players</h1>

<table>
  <tr>
	<th>Player</th>
    <th>Team</th>
<% if user_signed_in? %>
    <th></th>
    <th></th>
<% end %>
  </tr>

<% @players.each do |player| %>
  <tr>
    <td><%= link_to(player.last_and_first_name, player) %></td>
	<td><%= link_to( player.team.team_name, team_path(player.team.id) )%></td>
<% if user_signed_in? %>
    <td><%= link_to 'Edit', edit_player_path(player) %></td>
    <td><%= link_to 'Destroy', player, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% end %> 
  </tr>
<% end %>
</table>
<%= will_paginate @players %>
<br />
<% if user_signed_in? %>
<%= link_to 'New Player', new_player_path %>
<% end %>
